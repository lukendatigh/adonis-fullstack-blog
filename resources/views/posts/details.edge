@layout('main')

@section('content')
	<a href="/posts">Go back</a>
	<hr>
	<h1>{{ post.title }}</h1>
	<p>{{ post.body }}</p>
	<hr>
	<form action="{{ '/posts/' + post.id + '?_method=DELETE'}}" method="POST">
		{{ csrfField() }}
		<button type="submit" class="btn btn-danger">Delete</button>
	</form>
	
	<a href="/posts/edit/{{ post.id }}" class="btn btn-dark">Edit Post</a>
@endsection